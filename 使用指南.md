# 多用户待办事项管理系统

## 📋 项目简介

一个功能完整的多用户待办事项管理应用，支持实时协作、离线同步、冲突处理等高级特性。

## 🚀 快速开始

### 环境要求
- Python 3.8+
- Node.js 16+
- PostgreSQL (可选，使用SQLite开发)

### 一键启动

#### Windows
```bash
# 启动完整项目
start.bat

# 或分别启动
cd backend && python -m uvicorn app.main:app --reload
cd frontend && npm run dev
```

#### Linux/Mac
```bash
chmod +x start.sh
./start.sh
```

### Docker部署
```bash
docker-compose up -d
```

## 🎯 核心功能

### ✅ 基础功能
- [x] 用户注册/登录
- [x] 任务创建/编辑/删除
- [x] 任务分类和优先级
- [x] 子任务支持

### 🤝 协作功能
- [x] 共享清单
- [x] 任务分配
- [x] 实时评论
- [x] 协作状态显示

### 🌐 离线支持
- [x] 离线操作记录
- [x] 断网自动同步
- [x] 冲突检测与解决
- [x] 数据一致性保证

### 🔒 安全特性
- [x] JWT认证
- [x] 权限分级控制
- [x] 数据隐私保护
- [x] 操作审计日志

## 🏗️ 技术架构

### 前端 (Vue 3)
- **框架**: Vue 3 + Composition API
- **状态管理**: Pinia
- **UI组件**: Element Plus
- **构建工具**: Vite
- **实时通信**: WebSocket

### 后端 (FastAPI)
- **框架**: FastAPI (Python)
- **数据库**: SQLAlchemy ORM
- **认证**: JWT + OAuth2
- **部署**: Uvicorn + Gunicorn

### 数据库设计
- **主表**: 用户、任务、评论
- **关系表**: 任务分配、共享清单
- **扩展表**: 离线操作、冲突记录

## 🔧 API文档

访问 `http://localhost:8000/docs` 查看交互式API文档

### 主要API端点

```bash
# 认证相关
POST /api/auth/register     # 用户注册
POST /api/auth/login        # 用户登录

# 任务管理
GET    /api/todos           # 获取任务列表
POST   /api/todos           # 创建任务
PUT    /api/todos/{id}      # 更新任务
DELETE /api/todos/{id}      # 删除任务

# 协作功能
POST   /api/shared-lists    # 创建共享清单
POST   /api/comments        # 添加评论
POST   /api/task-assignments # 分配任务

# 离线同步
POST   /api/offline-sync/operations # 提交离线操作
GET    /api/offline-sync/conflicts  # 获取冲突列表
```

## 📱 前端组件

### 核心组件
- `TodoList.vue` - 任务列表管理
- `CommentsSection.vue` - 评论系统
- `SharedListsManager.vue` - 共享清单管理
- `OfflineIndicator.vue` - 离线状态指示

### 协作组件
- `CollaborationPresence.vue` - 在线用户显示
- `ConflictNotification.vue` - 冲突提醒
- `PrivacyControl.vue` - 隐私设置

### 同步组件
- `FullDataSyncManager.vue` - 全量数据同步
- `ProgressiveSyncDialog.vue` - 渐进式同步

## 🔧 开发指南

### 项目结构
```
todo-app/
├── backend/              # 后端服务
│   ├── app/
│   │   ├── api/         # API路由
│   │   ├── models/      # 数据模型
│   │   ├── schemas/     # 数据验证
│   │   └── utils/       # 工具函数
│   └── requirements.txt # Python依赖
├── frontend/             # 前端应用
│   ├── src/
│   │   ├── components/  # Vue组件
│   │   ├── views/       # 页面视图
│   │   ├── stores/      # 状态管理
│   │   └── composables/ # 组合式函数
│   └── package.json     # Node依赖
└── docs/                # 文档资料
```

### 开发环境配置

1. **后端开发**
```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# 或 .venv\Scripts\activate  # Windows
pip install -r requirements.txt
python -m uvicorn app.main:app --reload
```

2. **前端开发**
```bash
cd frontend
npm install
npm run dev
```

### 数据库迁移
```bash
# 初始化数据库
python backend/check_tables.py

# 添加新字段
python backend/migrate_timestamp_fields.py
```

## 🧪 测试

### 功能测试
```bash
# 冲突处理测试
python test_conflict_handling.py

# 离线同步测试
python test_offline_sync.py

# 子任务功能测试
python test_subtask_functionality.py

# 压力测试
python system_pressure_test.py
```

### API测试
访问前端测试页面：
- `http://localhost:5173/test.html` - 基础功能测试
- `http://localhost:5173/public/api-test.html` - API接口测试

## 🚀 部署指南

### 生产环境部署

1. **环境变量配置**
```bash
# .env.production
DATABASE_URL=postgresql://user:pass@localhost/todo
SECRET_KEY=your-secret-key
DEBUG=False
```

2. **后端部署**
```bash
cd backend
pip install -r requirements.txt
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker
```

3. **前端构建**
```bash
cd frontend
npm run build
# 部署 dist/ 目录到Nginx/Apache
```

### Docker部署
```bash
docker-compose up -d
```

## 📊 性能指标

### 当前性能
- **并发用户**: 1,000 (开发环境)
- **响应时间**: < 50ms
- **成功率**: 99.9%
- **内存占用**: < 100MB

### 扩展能力
- **目标用户**: 100,000+
- **QPS目标**: 10,000+
- **连接数**: 50,000+ WebSocket

## 🔒 安全特性

### 认证授权
- JWT Token认证
- 权限分级控制
- 操作审计日志

### 数据保护
- 敏感数据加密存储
- SQL注入防护
- XSS攻击防护

## 🤝 贡献指南

1. Fork项目
2. 创建功能分支
3. 提交更改
4. 发起Pull Request

## 📄 许可证

MIT License

## 📞 技术支持

如有问题，请提交Issue或联系项目维护者。